# Pass 4 materialization trigger

This file exists only as a clean PR trigger for the trusted Pass 4 asset materialization workflow. The workflow downloads the existing prototype asset payload into the repository and rewrites runtime references to local paths. It can be removed after successful materialization.