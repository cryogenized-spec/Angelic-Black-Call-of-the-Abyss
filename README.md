# Angelic Black — Call of the Abyss

This repository contains the Angelic Black game build and its Phaser migration workspace.

The active web build lives under `phaser/` and is the Phaser 4.2.1 migration target. The legacy HTML runtime was retained only during migration and is not the intended production entry point.